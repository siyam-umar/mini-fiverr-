<%= form_with(model: [conversation, message], local: false) do |form| %>
  <div class="input-group">
    <%= form.text_field :body, class: "form-control", placeholder: "Type a message..." %>
    <button class="btn btn-primary" type="submit">Send</button>
  </div>
<% end %>
